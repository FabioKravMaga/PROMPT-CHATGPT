# Precatório Automation Backend

This is a minimal FastAPI backend serving as a starting point for an automated workflow for managing precatório leads and proposals.

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Testing

```bash
pytest
```
